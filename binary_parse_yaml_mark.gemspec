Gem::Specification.new do |spec|
  spec.name = "binary-parse-yaml-mark"
  spec.version = "0.1.0"
  spec.summary = "Implement a Ruby parsers project for yaml diagnostic reporting, using negative fixtures and human-readable error snapshots."
  spec.files = Dir["lib/**/*.rb"]
  spec.required_ruby_version = ">= 3.0"
end
